<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ('../partials/head') %>
    <link rel="stylesheet" href="/css/productShow.css">
</head>

<body>
    
    <div class="container">
        <!-- Header de header.ejs -->
        <%- include ('../partials/header') %>
    <main class="fonditos paovh80">
        
        <section class="container">
            <article class="row d-flex justify-content-center">
                <figure class="card-img-top col-12 col-md-7 bg-white mt-5 m-0 pt-3 sombra">
                    <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">

                        <% if (productToShow.images.length > 1) { %>
                        <ol class="carousel-indicators">
                            <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
                            <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
                            <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
                            <li data-target="#carouselExampleIndicators" data-slide-to="3"></li>
                        </ol>
                        <% }; %>

                        <div class="carousel-inner">
                            <% for (i=0;i<productToShow.images.length;i++) { %>
                            <div class="carousel-item <% if (i>0) { %> <%= "" %> <% } else { %> <%= "active" %> <% } %>">
                                <figure class="d-flex justify-content-center align-items-center">
                                    <img class="mm-imgLimit" src="/img/prod_img/<%= productToShow.images[i] %>.jpg"
                                        alt="...">
                                    <!-- <img src="https://images.unsplash.com/photo-1533856493584-0c6ca8ca9ce3?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1950&q=80" class="img-fluid"  alt="..."> -->
                                </figure>
                            </div>
                            <% }; %>
                        </div>
                        <% if (productToShow.images.length > 1) { %>
                        <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                            <span class="fas fa-angle-left MM-carousel-control-color" aria-hidden="true"></span>
                            <span class="sr-only">Previous</span>
                        </a>
                        <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                            <span class="fas fa-angle-right MM-carousel-control-color" aria-hidden="true"></span>
                            <span class="sr-only">Next</span>
                        </a>
                        <% }; %>
                    </div>
                </figure>
    
                
    
    <!--
                <figure class="card-img-top col-12 col-md-7 bg-white mt-5 m-0 pt-3">
                    <img class="w-100" src="/img/products/patines/inline/ps-powerskating-r4-lg.jpg" alt="">
                </figure>
    -->
                
                
                
                <section class="card-body col-12 col-md-5 bg-white m-0 mt-5 p-3 sombra">
                    <div class="card-title text-center pt-5">
                        <h2><%= productToShow.marca + ' ' + productToShow.modelo %></h2>
                    </div>
                    <div class="cardPrecio text-center">
                        <p>$<%= productToShow.precio %></p>
                    </div>
                    <form class="form-group" action="/cart/additemCart/<%= productToShow.id%>" method="POST">
                        <label class="my-1" for="sel1">Color:</label>
                        <select class="form-control" id="sel1">
                            <option selected disabled>-elegir color-</option>
                            <option value="<%= productToShow.color %>"><%= productToShow.color %></option>
                        </select>
                            <label class="my-1" for="sel1">Talle:</label>
                            <select class="form-control mb-4" id="sel1">
                                <option selected disabled>-elegir talle-</option>
                                <% for (i=0;i<productToShow.talle.length;i++) { %>
                                <option value="<%= productToShow.talle[i] %>"><%= productToShow.talle[i] %></option>
                                <% }; %>
                            </select>
                        <input class="w-100 botcar" type="submit" value="Agregar al carrito">
                        <a type="button" class="w-100 botcar text-center" href="/category/<%=productToShow.id_categoria%>">Volver</a>
                    </form>
                </section>
                <section class="row col-12 bg-white mt-1 d-flex justify-content-center sombras">
                    <p>
                        <%= productToShow.descripcion %>
                    </p>
                    <p>
                        The Powerslide Phuzion Xenon Men Triskate is a high-performance fitness skate. Constructed using a 
                        glass-fiber reinforced outer shell and cuff and combined with the TRINITY mounting system it provides 
                        an amazing stable platform that improves power transfer, reduces vibrations and creates a super low 
                        center of gravity for more and better balance. 

                    </p>
                </section>
            </article>
        </section>
    </main>
    <!-- Footer de footer.ejs -->
    <%- include ('../partials/footer') %>

    <!-- Scripts de scripts.ejs -->
    <%- include ('../partials/scripts') %>

    <!-- Fin del Container -->
    </div>

</body>
</html>